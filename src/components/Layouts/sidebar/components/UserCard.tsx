import React from "react";
import { IUserCard } from "../types";

export const UserCard = ({}:IUserCard) => {
  return (
    <p className="hover:bg-slate-400 rounded cursor-pointer h-[5dvh]">
      usercard
    </p>
  );
};

export default UserCard;
